pipeline {
  agent any 
    stages{
      stage("frontend") {
        steps {
          echo 'Build the application'
        }
      }
      stage("Backend") {
        steps {
          echo 'Test the application'
        }
      }
    }
}
